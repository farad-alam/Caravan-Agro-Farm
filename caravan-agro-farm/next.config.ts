import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'caravanagrofarm.com'], // Add your domain if you have one
  },
};

export default nextConfig;
